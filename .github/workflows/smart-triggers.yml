name: Smart Triggers

on:
  push:
    branches:
      - main
      - 'release/*'
    paths:
      - 'src/**'
      - 'app/**'

jobs:
  run-if-code-changes:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Code changes detected 🚀"
